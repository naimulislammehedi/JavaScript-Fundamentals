// old way 
function greet(name) {
    return "Hello " + name; 
}

// ES6 
const greet = (name) => {
    return "Hello " + name; 
}

const greet = name => "Hello " + name; 
console.log(greet("Mehedi")); 
