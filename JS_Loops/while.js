// Use it when you don't know exactly how many times the loop will run 
/*
// Syntax 

while (condition) {
    // code 
}
*/

let count = 1; 

while (count <= 5) {
    console.log(count); 
    count++; 
}